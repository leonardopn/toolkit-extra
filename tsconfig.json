{
    "compilerOptions": {
        "target": "es2017", // Defina a versão do JavaScript alvo para a sua biblioteca.
        "module": "commonjs", // Especifique o código do módulo gerado.
        "strict": true, // Ative todas as opções de verificação de tipo estritas.
        "esModuleInterop": true, // Emita JavaScript adicional para facilitar a importação de módulos CommonJS.
        "forceConsistentCasingInFileNames": true, // Garanta que o uso de maiúsculas e minúsculas esteja correto nas importações.
        "declaration": true, // Gere arquivos .d.ts a partir dos arquivos TypeScript.
        "outDir": "./dist", // Especifique o diretório de saída para todos os arquivos gerados.
        "declarationDir": "./dist/types", // Especifique o diretório de saída para os arquivos .d.ts gerados.
        "noUnusedLocals": true, // Ative a verificação de variáveis locais não utilizadas.
        "noUnusedParameters": true, // Ative a verificação de parâmetros de função não utilizados.
        "noImplicitReturns": true, // Ative a verificação de caminhos de código que não retornam explicitamente em uma função.
        "noFallthroughCasesInSwitch": true, // Ative a verificação de casos de queda em declarações switch.
        "noUncheckedIndexedAccess": true, // Adicione 'undefined' a um tipo quando acessado usando um índice.
        "strictPropertyInitialization": true // Verifique as propriedades da classe que são declaradas, mas não são definidas no construtor.
    },
    "include": [
        "src" // Especifique a pasta onde seus arquivos de origem estão localizados.
    ],
    "exclude": [
        "node_modules" // Exclua a pasta node_modules da compilação.
    ]
}
